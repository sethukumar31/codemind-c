#include<stdio.h>
int main()
{
    int n,r,sq,sum=0;
    scanf("%d",&n);
    sq=n*n;
    while(sq!=0)
    {
        #include<stdio.h>
int main()
{
    int n,r,sq,sum=0,x;
    scanf("%d",&n);
    sq=n*n;
    x=sq;
    while(sq!=0)
    {
        r=sq%10;
        sum+=r;
        sq=sq/10;
    }
    if(n==sum)
      printf("Neon Number");
    else
      printf("Not Neon Number");
    return 0;
}
    }
    if(n==sum)
      printf("Neon Number");
    else
      printf("Not Neon Number");
    return 0;
}